package com.redalpha.callsmanager.dao;

import com.redalpha.callsmanager.domain.Call;

/**
 * Represents behavior for managing calls.
 */
public interface CallRepository extends BaseRepository<Call, Integer> {
}
