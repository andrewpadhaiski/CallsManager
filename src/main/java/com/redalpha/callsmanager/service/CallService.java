package com.redalpha.callsmanager.service;

import com.redalpha.callsmanager.domain.Call;

/**
 * Represents behavior for managing calls.
 */
public interface CallService extends BaseService<Call, Integer> {
}
